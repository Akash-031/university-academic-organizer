# Backend environment variables. Keep this file as an example only; do not add secrets.
PORT=5000
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:5173,https://university-academic-organizer.onrender.com
NODE_ENV=production
# Server-only secrets. Never prefix these with VITE_ or expose them to the browser.
SUPABASE_URL=
SUPABASE_SECRET_KEY=
FRONTEND_URL=
RESET_EMAIL_FROM=
RESET_EMAIL_HOST=
RESET_EMAIL_PORT=
RESET_EMAIL_USER=
RESET_EMAIL_PASSWORD=
