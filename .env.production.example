# Frontend build environment variables. Values prefixed with VITE_ are public in the built app.
# Leave empty for a unified Express deployment, where the browser calls same-origin /api.
# Set to https://your-api-domain.example only when hosting the Vite frontend separately.
VITE_API_BASE_URL=
